name: Run LNG Pricing Script Daily

on:
  schedule:
    - cron: '30 2 * * 1-5'  # 06:30 Abu Dhabi time, Monday–Friday
  workflow_dispatch:

jobs:
  run-script:
    runs-on: ubuntu-latest

    env:
      EMAIL_PASSWORD: "pjyqifgeyxbbqquv"

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install selenium beautifulsoup4 pandas numpy requests python-dateutil lxml xlsxwriter

      - name: Run script
        run: python LNG_pricing_sheet_construction.py
